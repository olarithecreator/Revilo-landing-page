import React from "react";
import { Link } from "react-router-dom";

export default function TemplateCard({ template, isLocked, comment, onDownload, showFreeBadge, showLockedInfo, onUpgradeClick }) {
  return (
    <div className={`bg-white rounded-xl p-5 text-center shadow-md transition-all relative overflow-hidden ${isLocked ? 'filter grayscale' : ''}`}>
      {/* Free Forever Badge */}
      {showFreeBadge && (
        <span className="absolute top-2 right-2 bg-green-500 text-white text-xs font-bold px-2 py-1 rounded">Free Forever</span>
      )}
      {/* Pro Badge for locked templates */}
      {isLocked && (
        <span className="absolute top-2 left-2 bg-purple-600 text-white text-xs font-bold px-2 py-1 rounded animate-pulse">Pro</span>
      )}
      <div className="relative">
        <img src={template.image} alt={template.type} className={`w-full mb-3 rounded ${isLocked ? 'blur-sm' : ''}`} />
        {/* Blurred overlay for locked templates */}
        {isLocked && (
          <div className="absolute inset-0 bg-white bg-opacity-60 rounded"></div>
        )}
      </div>
      <h3 className="font-bold text-lg mb-1">{template.type}</h3>
      <p className="text-xs italic min-h-[40px] mb-2">{comment}</p>
      <p className="text-xs mb-2">{template.description}</p>
      <p className="text-xs mt-2 opacity-60">Generated by revilo</p>
      {isLocked ? (
        <>
          {showLockedInfo && (
            <div className="text-xs text-red-600 mb-2">Only the Black template is free after your 3 free downloads. Upgrade to unlock more.</div>
          )}
          <button disabled className="mt-3 bg-gray-300 text-white py-2 px-4 rounded w-full cursor-not-allowed mb-2">🔒 Locked</button>
          <Link
            to="/pricing"
            className="block mt-1 bg-gradient-to-r from-pink-500 to-purple-600 text-white font-bold py-2 px-4 rounded w-full shadow-lg hover:from-purple-600 hover:to-pink-500 transition transform hover:scale-105 animate-bounce"
          >
            Upgrade
          </Link>
        </>
      ) : (
        <button onClick={onDownload} className="mt-3 bg-indigo-900 text-white py-2 px-4 rounded w-full hover:bg-indigo-800 transition">Download</button>
      )}
    </div>
  );
} 